# GridironIQ Python dependencies.
# Canonical source is pyproject.toml; this mirror is for `pip install -r`.
# (For an editable install of the package itself: `pip install -e .`)

# data + storage
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
requests>=2.31
SQLAlchemy>=2.0

# modeling / viz (Boardroom)
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
seaborn>=0.13

# Film Room natural-language layer
anthropic>=0.40
python-dotenv>=1.0

# app
streamlit>=1.30
